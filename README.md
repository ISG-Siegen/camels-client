This is the generic CaMeLS client.  
Set the server IP in the connection_settings.json to connect with a compatible CaMeLS server.  
Run validate_connection.py to download the latest Enums from the server (performed automatically on requests).  
Implement the master wrapper and one wrapper file for each supported library.  
